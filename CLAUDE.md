# 太阳系 3D 交互模型

## 技术栈
Vite + Three.js (原生 JS，不用 React/TS)。所有 UI 用 HTML overlay，不在 canvas 内绘制。

## 铁律（违反即视为错误实现）
1. 单位约定：所有天体数据一律以真实物理单位存储（距离 km，时间 秒，角度 度）。
   渲染时通过 src/core/scale.js 的转换函数换算成场景单位，禁止在数据文件里预先缩放。
2. 时间基：全局唯一时间源是 J2000 儒略日（JD 2451545.0 起算）。
   自转、公转、UI 日期显示必须共用同一个 simulationTime，禁止各模块自己维护时间。
3. 数据与逻辑分离：星球参数、探测任务、导览脚本一律放 /data/*.json，
   引擎代码不得硬编码任何具体天体的数值或文案。
4. 尺度是作弊的，且必须显式：任何缩放系数都从 scale.js 读取，
   并且 UI 上要能实时显示当前的距离压缩比和半径放大倍数。
5. 相机位置与朝向分开插值，禁止每帧直接调用 lookAt 硬指目标。

## 目录结构
src/core/     引擎（时间、尺度、相机、渲染器）
src/bodies/   天体构建与轨道计算
src/ui/       HTML overlay 界面
src/tour/     导览脚本引擎
data/         JSON 数据
public/textures/  贴图资源

## 开发约定
- 每个阶段完成后必须能 npm run dev 直接看到效果，禁止留半成品占位。
- 新增依赖前先问我。